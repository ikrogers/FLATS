<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="">FLATS <i class="fa fa-bicycle fa-fw"></i></a>
        </div>
        
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li><%= link_to('<i class="fa fa-home fa-fw"></i> Home'.html_safe, authenticated_root_path) %></li>
            </ul>
            
            <ul class="nav navbar-nav navbar-right">
                <% if user_signed_in? %>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <% if current_user.gender == "male" %>
                            <i class="fa fa-male fa-fw"></i>
                        <% else %>
                            <i class="fa fa-female fa-fw"></i>
                        <% end %>
                        <%= current_user.email %> <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <li><%= link_to('<i class="fa fa-pencil fa-fw"></i> Edit'.html_safe, edit_user_registration_path) %></li>
                            <li><%= link_to('<i class="fa fa-plus fa-fw"></i> Add Exercise'.html_safe, "https://flats-c9-whytesensei.c9.io/exercise_managers/new") %></li>
                            <li class="divider"></li>
                            <li><%= link_to('<i class="fa fa-ban fa-fw"></i> Logout'.html_safe, destroy_user_session_path, :method => :delete) %></li>
                        </ul></a>
                    </li>
                <% else %>
                <li><%= link_to('Register', new_user_registration_path) %></li>
                <% end %>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>